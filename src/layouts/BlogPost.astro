---
import BaseLayout from './BaseLayout.astro';
import TableOfContents from '../components/TableOfContents.astro';
import Comments from '../components/Comments.astro';

interface Props {
  title: string;
  description: string;
  pubDate: Date;
  updatedDate?: Date;
  tags?: string[];
  content: string;
  headings?: Array<{ depth: number; slug: string; text: string }>;
}

const { title, description, pubDate, tags = [], content, headings = [] } = Astro.props;
const formattedDate = pubDate.toISOString().split('T')[0];
---

<BaseLayout title={title} description={description}>
  <article class="max-w-5xl mx-auto px-4 py-12">
    <header class="mb-12">
      <div class="flex flex-col gap-4 items-start slide-in" style="--delay: 0s;">
          <a 
          href="/blog" 
          class="back-button inline-flex items-center gap-2 no-underline hover:bg-transparent"
          >
          <span>←</span>
          <span>BACK</span>
        </a>
        
        <span class="date-badge">
          {formattedDate}
        </span>
      </div>
      
      <h1 class="post-title mt-6 mb-6 slide-in" style="--delay: 0.1s;">
        {title}
      </h1>
      
      <p class="text-xl text-[var(--color-muted)] leading-relaxed slide-in" style="--delay: 0.2s;">
        {description}
      </p>
    </header>
    
    <hr class="border-t-2 border-[var(--color-border)] mb-12 slide-in" style="--delay: 0.3s;" />
    
    <div class="lg:flex lg:gap-12 slide-up" style="--delay: 0.4s;">
      <aside class="hidden lg:block lg:w-56 lg:shrink-0">
        <div class="sticky top-24">
          <TableOfContents headings={headings} />
        </div>
      </aside>
      
      <div class="flex-1 min-w-0">
        <div class="lg:hidden mb-8">
          <TableOfContents headings={headings} />
        </div>
        
        <div class="prose">
          <slot />
        </div>
        
        <Comments />
      </div>
    </div>
  </article>
</BaseLayout>

<style>
  .back-button {
    display: inline-flex;
    padding: 0.5rem 1rem;
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 0.875rem;
    border: 2px solid var(--color-border);
    background-color: var(--color-background);
    transition: all 0.1s;
    box-shadow: var(--shadow-brutal);
  }

  .back-button:hover {
    transform: translate(2px, 2px);
    box-shadow: var(--shadow-brutal-md);
  }

  .date-badge {
    display: inline-block;
    padding: 0.25rem 0.75rem;
    font-family: var(--font-display);
    font-size: 0.875rem;
    font-weight: 600;
    background-color: #fa8cef;
    color: var(--color-brutal-black);
    border: 2px solid var(--color-border);
  }

  .post-title {
    font-family: var(--font-display);
    font-size: clamp(2.5rem, 8vw, 4rem);
    font-weight: 900;
    line-height: 1.1;
    text-transform: uppercase;
    letter-spacing: -0.02em;
  }

  .slide-in {
    opacity: 0;
    transform: translateX(-30px);
    animation: slideIn 0.6s ease-out forwards;
    animation-delay: var(--delay, 0s);
  }

  @keyframes slideIn {
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  .slide-up {
    opacity: 0;
    transform: translateY(30px);
    animation: slideUp 0.5s ease-out forwards;
    animation-delay: var(--delay, 0s);
  }

  @keyframes slideUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
